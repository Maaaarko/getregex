import React from "react"

const Heading = () => {
    return (
        <div className="heading">
            <h1>Get Regex</h1>
            <h2>Process natural language to get regular expressions</h2>
            <p>
                This website is using OpenAI's DaVinci engine to process natural
                language. Please note that this is an experimental feature so
                don't take the returned regular expressions for granted.
            </p>
        </div>
    )
}

export default Heading
