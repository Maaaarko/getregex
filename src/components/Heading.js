import React from "react"

const Heading = () => {
    return (
        <div className="heading">
            <h1>getregex.net</h1>
            <p>
                getregex.net is using OpenAI's DaVinci engine to process natural
                language. Please note that this is an experimental feature so
                don't take the returned regular expressions for granted.
            </p>
        </div>
    )
}

export default Heading
